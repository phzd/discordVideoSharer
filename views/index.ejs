<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PHZD</title>
    <link rel="icon" id="favicon" href="/images/favicon.png" type="image/png">

    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #1f1f1f;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;       /* horizontal center */
            justify-content: flex-start; /* top alignment */
            min-height: 100vh;
            text-align: center;
        }

        #main-content {
            background-color: #000000;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-top: 60px;
            width: 30%
        }

        /* Mobile styles */
        @media only screen and (max-width: 768px) {
            #main-content {
                background-color: #000000;
                padding: 40px;
                border-radius: 20px;
                box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
                margin-top: 60%;
                width: 80%
            }
        }

        h1 {
            color: #4f46e5;
        }

        p {
            color: #ffffff;
            font-size: 1.2em;
            line-height: 1.6em;
        }

        input {
            padding: 12px 16px;
            border: 2px solid #ddd;
            border-radius: 12px;   /* rounded edges */
            outline: none;
            font-size: 16px;
            transition: all 0.3s ease;
            width: 80%;
            background-color: #1f1f1f;
            color: white;
        }

        input:focus {
            border-color: #4f46e5; /* Indigo accent */
            box-shadow: 0 0 6px rgba(79, 70, 229, 0.4);
        }

        button {
            padding: 12px 20px;
            border: none;
            border-radius: 12px; /* rounded edges */
            background-color: #1e3a8a; /* deep blue */
            color: #ffffff; /* solid white text */
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        button:hover {
            background-color: #2b4fc7; /* brighter blue on hover */
            box-shadow: 0 0 8px rgba(79, 70, 229, 0.6); /* indigo glow */
        }

        button:active {
            transform: scale(0.97);
        }

        .optional {
            font-size: 0.85em;
            color: #aaa; /* lighter gray for subtlety */
            font-weight: normal;
            margin-left: 4px;
        }

        #username-box {
            position: absolute;
            top: 10px;
            left: 10px;
            background: #000000;
            padding: 10px;
            border-radius: 15px;
        }
        #username-box input {
            padding: 10px;

        }

        /* active (when radio is selected) */
        input[type="radio"]:checked + label.btn {
            background-color: #374151; /* darker gray when selected */
            box-shadow: inset 0 0 5px rgba(0, 0, 0, 0.5);
        }

        /* hide real radio inputs */
        input[type="radio"] {
            display: none;
        }

        /* style the label like button */
            label.btn {
            padding: 12px 20px;
            border: none;
            border-radius: 12px;
            background-color: #1e3a8a;
            color: #ffffff;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-right: 8px; /* spacing */
            margin-bottom: 8px;
            display: inline-block;
            text-align: center;
        }

        /* hover effect */
        label.btn:hover {
            background-color: #2b4fc7;
            box-shadow: 0 0 8px rgba(79, 70, 229, 0.6);
        }

        
        /* Mobile styles */
        @media only screen and (max-width: 768px) {
            body {
                justify-content: center; /* centers vertically on small screens */
            }

            #main-content {
                width: 85%;
                margin-top: 0; /* remove large top margin */
                padding: 30px; /* a bit tighter padding on mobile */
            }

            input, button, label.btn {
                font-size: 24px; /* make text/buttons larger */
                padding: 24px 32px; /* more touch-friendly */
            }

            p {
                font-size: 24px;
            }

            .optional {
                font-size: 18px;
            }

            #username-box {
                position: static; /* removes absolute positioning on mobile */
                margin-bottom: 20px;
                width: 85%;
                
            }
        }


    </style>
</head>
<body>
    <div id="username-box">
        <form action="/set-username" method="POST">
            <p>
                Shared by <span class="optional">(optional)</span>
            </p>
            <input 
                type="text" 
                name="username" 
                placeholder="Enter your name"
                value="<%= locals.username ? locals.username : '' %>"
            >
            <br><br>
            <button type="submit">Save</button>
        </form>
    </div>

    <div id="main-content">
        <form id="videoForm" onsubmit="this.querySelector('button').disabled = true;">
            <h1>Share a video to <%=locals.server%></h1>
            <% locals.channels.forEach(function(channel) { %>
                <input type="radio" id="<%=channel.name%>" name="channelSet" value="<%=channel.name%>" <% if (channel.name === locals.defaultChannel) { %>checked<% } %>>
                <label for="<%=channel.name %>" class="btn"><%=channel.name%></label>
            <% }); %>
            <p>Video you would like to share</p>
            <input class="input" type="text" name="video" id="videoInput" placeholder="https://www.youtube.com/watch?v=dQw4w9WgXcQ">
            <br><br><br>
            <p>
                Message <span class="optional">(optional)</span>
            </p>
            <input class="input" type="text" name="message" id="messageInput">
            
            <br><br>
            <button type="submit">Send</button>
        </form>
    </div>

    
    <script>
        const form = document.getElementById('videoForm');
        form.addEventListener('submit', function(e) {
            e.preventDefault();

            const videoInput = document.getElementById('videoInput').value.trim();
            const messageInput = document.getElementById('messageInput').value.trim();

            if (!videoInput) {
                alert('Please enter a video URL');
                return;
            }

            let url = `/${videoInput}`;

            // Start building query params
            let params = new URLSearchParams();

            // Add message if it exists
            if (messageInput) {
             params.append("message", messageInput);
            }

            // Get selected radio
            let selectedOption = document.querySelector('input[name="channelSet"]:checked');
            if (selectedOption) {
              params.append("channel", selectedOption.value);
            }

            // If we have any params, attach them to the URL
            if (params.toString()) {
                url += `/?${params.toString()}`;
            }

            window.location.href = url;
        });
    </script>
</body>
</html>